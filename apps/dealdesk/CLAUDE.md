# dealdesk — CLAUDE.md

**Status:** SCAFFOLD. `docs/workflows/` holds workflow SOPs; `web/` is an empty Next.js shell (no source yet). Nothing deployed, nothing wired.

**Purpose:** deal-workspace surface (per the dir's own docs/workflows). Built on the shared deal head (`deals` tables) when development resumes — note `apps/deal44/` owns the multi-party room layer; check for overlap before building here.

**Envs:** none referenced yet; O5 applies from the first reference.

**Deploy target:** Vercel (`apps/dealdesk`) when there is code to deploy.
