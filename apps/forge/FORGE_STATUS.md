# FORGE REBUILD — Status Report
**Date:** April 16, 2026
**Session:** Complete Rebuild (Security + APIs + Redesign + SEO)

---

## 1. Security: /.env Access

**Status: FIXED**
- `.env.forge.prod`, `.env.prod.secrets`, `.env.production`, `.env.pull` removed from git
- `next.config.js` rewrites block all `/.env*` URLs → 404
- `.gitignore` updated with `/.env.*` catch-all pattern
- Commit: `f787446`

**ACTION REQUIRED:** Rotate all exposed keys (they remain in git history of the source forge repo):
- `ANTHROPIC_API_KEY` ([REDACTED — see canonical vault])
- `SUPABASE_SERVICE_ROLE_KEY` ([REDACTED])
- `RESEND_API_KEY` ([REDACTED])
- `ETHERSCAN_API_KEY` (K969...)
- `PAYPAL_CLIENT_ID` (ARaoP...)

---

## 2. API Endpoints

| Endpoint | Method | Status | Notes |
|---|---|---|---|
| `/api/scan` | POST | WORKING | Claude AI analysis + Apify + mock fallback. Returns preview (2 findings). |
| `/api/scan/checkout` | POST | WORKING | Creates NOWPayments invoice + Payoneer link. Column name fixed. |
| `/api/boi-order` | POST | NEW | Saves BOI order, sends Resend email + Telegram alert. |
| `/api/payment/crypto` | POST | WORKING | Creates NOWPayments invoice for any product. |
| `/api/payment/webhook` | POST | WORKING | IPN handler — marks scans/passports as paid. |
| `/api/payment/status` | GET | WORKING | Query payment status by scan/passport ID. |
| `/api/lead-magnet` | POST | NEW | Gap page lead capture → Supabase + Resend + Telegram. |
| `/api/newsletter` | POST | NEW | Newsletter signup → Supabase upsert + Resend welcome email. |

---

## 3. Payment Integration

| Product | PayPal | NOWPayments (Crypto) | Payoneer (Card) |
|---|---|---|---|
| BOI Report ($149) | Via `/api/boi-order` | Via `/api/payment/crypto` | Via env link |
| Web Scanner ($97-119) | N/A | Via `/api/scan/checkout` | Via env link |
| Regulatory Passport ($297-347) | N/A | Via `/api/payment/crypto` | Via env link |

**PayPal Smart Buttons:** Not yet integrated on BOI page (requires `NEXT_PUBLIC_PAYPAL_CLIENT_ID` env var + Script tag). Currently BOI uses NOWPayments + Payoneer.

---

## 4. Scanner Functionality

The `/api/scan` route accepts URL + email + vertical (11 compliance types) and returns:
- Risk level (low/medium/high/critical)
- Exposure range ($min — $max)
- Preview findings (first 2 of N)
- Full report behind paywall

The scanner uses Claude AI for analysis with Apify web scraping as the data source. Falls back to mock data when Apify is not configured.

---

## 5. SEO Coverage

| Page | Title | Description | OG Tags | Canonical |
|---|---|---|---|---|
| Homepage | YES | YES | YES | YES |
| /boi | YES (via layout.tsx) | YES | YES | YES |
| /audit | YES (via layout.tsx) | YES | YES | YES |
| /pricing | YES (via layout.tsx) | YES | YES | YES |
| /terms | YES | YES | -- | -- |
| /privacy | YES | YES | -- | -- |
| /disclaimer | YES | YES | -- | -- |

---

## 6. Sitemap & Robots

- **`/sitemap.xml`** — Dynamic sitemap with 8 static pages + all gap_pages from Supabase
- **`/robots.txt`** — Allows all crawlers on `/`, blocks `/api/` and `/_next/`

---

## 7. Supabase Tables Created

| Table | DB1 (ydghh) | DB2 (rgbwl) | RLS |
|---|---|---|---|
| `gap_pages` | YES | YES | Public read, service_role write |
| `boi_orders` | YES | YES | Service role only |
| `newsletter_subscribers` | YES | YES | Service role only |
| `scans` | existed | existed | Service role only |

---

## 8. Remaining Issues

### P0 — Must fix
1. **Rotate exposed API keys** — Anthropic, Supabase service role, Resend, Etherscan, PayPal keys were in committed `.env` files. Removed from git but still in history.
2. **Hetzner server unreachable** — 204.168.209.235 not responding to ping/SSH. Firewall or public network issue. n8n pipeline cannot be activated until server access is restored.
3. **PayPal Smart Buttons** — Not yet rendered on BOI page. Need to add PayPal JS SDK script tag and button initialization.

### P1 — Should fix next session
4. **Apify actors** — Scanner works with mock data but needs real Apify actors for production scanning
5. **BOI report delivery** — Order is captured but actual report generation/delivery pipeline not built
6. **Gap page population** — `gap_pages` table is empty. Need to run first HEARTBEAT via n8n or manual insert.

---

## Commits (this session)

| Hash | Description |
|---|---|
| `f787446` | CRITICAL: Block .env public access, remove committed secrets |
| `a82fd5c` | Phase 1: BOI order API, success page, Supabase migrations |
| `bccd0e2` | Phase 2+3: Homepage hero redesign, SEO metadata layouts |
| `befa676` | Phase 2+3: Sitemap, robots, newsletter API, pricing SEO, cleanup |

---

*Generated by Claude Opus 4.6 — Forge Complete Rebuild Session*
