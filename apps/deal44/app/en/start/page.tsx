import IntakeForm from '../../start/IntakeForm'

export const metadata = {
  title: 'Set up a deal room — DEAL44',
  description: 'Answer five things. You get a written proposal back, no call.',
}

export default function EnglishStartPage() {
  return <IntakeForm locale="en-US" />
}
