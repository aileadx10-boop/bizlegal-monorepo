# Week 3 readiness — 2026-05-09 final report

**Session goal:** "fix everything... main + subdomains all functional and working, OCI is working, Hetzner and Forge, all domains. Subdomains deployed. Do all so we can go week 3 and 4."

**Status:** ✅ achieved on Vercel + OCI; ⚠️ Hetzner ssh + Supabase DDL still need Moses; programmatic curator pipeline stalled.

---

## Production state (verified 2026-05-09 ~22:30 UTC)

| Surface | Path | HTTP | Notes |
|---|---|---|---|
| hub | `bizlegal-ai.com/` | 200 | dpl_7MdAEdyK58Ef7h6DFEskaEGwww1i — Globe.tsx ts-fix landed |
| hub | `bizlegal-ai.com/triage` | 200 | meta-router live |
| lexaudit | `lexaudit.bizlegal-ai.com/` | 200 | LandingV2 + SiteShell + Twilight |
| lexaudit | `/decision-tree` | 200 | V1 lead magnet #1 live |
| lexaudit | `/privacy` | 200 | shell on every route |
| brai | `brai.bizlegal-ai.com/` | 200 | Royal Dark theme |
| brai | `/decision-tree` | 200 | V1 lead magnet #5 live |
| brai | `/privacy` | 200 | |
| tracr | `tracr.bizlegal-ai.com/` | 200 | Royal Dark theme |
| tracr | `/decision-tree` | 200 | V1 lead magnet #2 live |
| tracr | `/privacy` | 200 | |
| docai | `docai.bizlegal-ai.com/` | 200 | was 500 (styled-jsx) → fixed via outputFileTracingRoot |
| docai | `/decision-tree` | 200 | V1 lead magnet #3 live |
| docai | `/privacy` | 200 | |
| leadforge | `leadforge.bizlegal-ai.com/` | 200 | LeadForgeLanding kept |
| leadforge | `/decision-tree` | 200 | V1 lead magnet #6 live |
| leadforge | `/privacy` | 404 | no /privacy page in this app |
| forge | `forge.bizlegal-ai.com/` | 200 | corepack-pnpm + a11y retrofit deployed |
| forge | `/decision-tree` | 200 | V1 lead magnet (Forge BOI) live |
| forge | `/privacy` | 200 | |
| OCI router | `router.bizlegal-ai.com/health` | `{ok:true,version:"v1.0.0-p1",redis:"up",supabase:"up"}` | rebuilt with email_contract.py + seed_partners.py + payout_reconciler.py |

**13 of 14 expected routes return 200.** The single 404 (leadforge `/privacy`) is "no page exists in code" — not a deploy failure; can be created when needed.

---

## What this session shipped (PRs to main)

1. PR #3 — vercel.json corepack pnpm + turbo (was broken `npm install`) — 5 subdomains
2. PR #4 — Forge a11y retrofit (A11Y-035)
3. PR #5 — Tier 1.5 silent-fail HIGH/MED
4. PR #6 — A11Y-034 toggle dedup + 4 HIGH comment fixes
5. PR #7 — Type-design F-1 (literal vars) + F-2 (discriminated Result)
6. PR #8 — F-3 applyTheme honest signature + M3 validator + 17 audit-code-comment rewrites
7. PR #9 — F-5 stickyLead optional + F-7 NavLink dedup
8. PR #10 — hub Globe.tsx + docai PrivacyScanDecisionTree.tsx tsc fixes
9. PR #11 — forge cmdk Command.GroupHeading + docai force-dynamic + Moses MVP funnel edits
10. PR #12 — docai lazy-init Supabase clients
11. PR #13 — hub Globe second ref + forge lead-magnet lazy supabase
12. PR #14 — forge Anthropic + Resend lazy-init
13. PR #15 — forge sitemap resilient + docai outputFileTracingRoot to monorepo root
14. PR #16 — forge "AI generates" marketing claim removed

Plus the original session (PR #1, #2, and the Phase AA homepages 442fffb).

**16 PRs merged. ~30 commits.**

---

## Blocker fixes done autonomously

- **Vercel project rootDirectory wrong**: docai-frontend was `apps/docai` (should be `apps/docai/web`) and forge was `apps/forge` (should be `apps/forge/apps/web`) — patched via API.
- **Forge git linkage severed**: re-linked via `POST /v9/projects/<id>/link`.
- **Vercel deploy hooks (env file) stale**: hub hook id deleted; forge project unlinked; both unusable. Used direct `POST /v13/deployments` with gitSource instead.
- **OCI router code outdated**: 3 missing Phase AA python files (`email_contract.py`, `seed_partners.py`, `payout_reconciler.py`) shipped via `tarball + scp + sudo extract + docker compose up --build`. Container healthy, image rebuilt.
- **Build failures** (this session diagnosed + fixed):
  1. Hub Globe.tsx Mesh ref drift (line 25 + 51)
  2. DocAI PrivacyScanDecisionTree comparison narrowing
  3. Forge cmdk `Command.GroupHeading` doesn't exist
  4. Forge `/api/lead-magnet` module-load createClient
  5. Forge `lib/claude` + `lib/resend` module-load instantiations
  6. DocAI module-load `supabaseAdmin` (lazy via Proxy, mirrors hub pattern)
  7. DocAI `outputFileTracingRoot` scoping → "Cannot find module 'styled-jsx'" runtime
  8. Forge sitemap.ts module-load createClient with required env

---

## What still needs Moses (Week 3 dependencies)

### Critical-path

| # | Item | Time | Why it blocks Week 3 |
|---|---|---|---|
| 1 | **Apply `picked_by` Supabase migration** | 30 sec dashboard | scout/auto_pick analytics quality. SQL: `services/hetzner/supabase/migration-daily-gaps-picked-by.sql` |
| 2 | **Hetzner scout systemd timer to daily + verify RSS feeds** | 10 min ssh | **Curator pipeline is stalled — only 4 raw items in last 7 days, 0 new gap pages since 2026-04-16.** Without daily cadence, W3 article-volume target collapses. Need Hetzner key (not in `.ssh/`) |
| 3 | **OCI partner outreach 3-5 emails** | 30-60 min | OCI revenue channel has zero partners → router has no one to route to → $0 OCI revenue |

### Soft / harden

- Turnstile site key + 5 Vercel env vars (W1 ops 20+21)
- `OCI_PARTNER_CC` env on OCI box (W1 op 24)
- `PAYPAL_WEBHOOK_ID` on hub Vercel preview (W1 op 25)
- Anthropic API key rotation (W1 op 9)

---

## Programmatic blog audit

- **gap_pages table:** 3 published pages, all from 2026-04-16
  - `us / fincen-travel-rule-defi-compliance` — risk_score 88
  - `us / fincen-kyc-defi-stablecoin-compliance`
  - `united-states / fincen-defi-bo-audit-guidance-2026`
- **daily_gaps table:** 7 raw scout items total, 4 from last 7 days
- **Forge `/gap/<jurisdiction>/<slug>` route:** ✓ HTTP 200 (programmatic page generator works)
- **Curator pipeline NOT producing new content** — pipeline is healthy in code, but Hetzner scout cadence is the blocker (W1 op #5/#6)

---

## Deferred (out of scope / time)

- **Hub Daybreak+Twilight design** — Moses asked for it, but hub has bespoke NavBar/Footer/CommandMenu/StickyConversionBar/etc. with its own `--bg`/`--bl-*` token system. Wiring in `@bizlegal/themes` ThemeProvider needs a focused redesign session that's out of scope here. Documented as next-session work.
- **HMAC replay protection (H-1)** — security-critical, touches handshake; needs ops monitoring during rollout.
- **CSP nonce middleware (S-2 / H5)** — multi-app platform task.
- **F-4 / F-6 / F-8 / F-9 / F-10 type-design MEDs** — focused PR's worth of refactors; not Week 3 blockers.

---

## TL;DR

✅ **Production is functional end-to-end.** All 7 Vercel projects rebuilt clean. OCI router updated with Phase AA code. V1 conversion machine (6 lead magnets + hub `/triage`) all live + reachable.

⚠️ **3 things need your physical action before content engine flows:**
1. Paste Supabase SQL migration (30 sec)
2. SSH Hetzner to flip scout to daily timer (5-10 min)
3. Send 3-5 OCI partner outreach emails (30-60 min)

After those: Week 3 starts cleanly with revenue path open + content cadence flowing.

---

*Generated 2026-05-09 by autonomous Phase AA finalize session.*
