# 09 — Hetzner box inventory, 2026-09-11 (evidence fold-in for FULL-SYSTEM-MAP)

**Agent:** Hetzner box + services inventory (`204.168.209.235`, `compliance-arbitrage`, Ubuntu 24.04, up 148d). All findings VERIFIED by direct command unless marked CONFIRMED.

## Security posture = ⚫ CRITICAL (the box is the biggest single exposure in the fleet)
- **No host firewall**: `ufw` → /dev/null, `fail2ban` → /dev/null. 🔶CONFIRMED
- Internet-reachable with no auth: **ollama :11434**, **netdata :19999** (+ proxied on :80), **n8n :5678** (basic-auth), **marimo :8080** (no auth). 🔶CONFIRMED
- **Plaintext secrets in `docker-compose.yml`** (world-readable): n8n basic-auth password + `N8N_ENCRYPTION_KEY`. 🔶CONFIRMED — rotate both.
- **`ghp_…` token embedded inline in `.git/config` remote URL** of `/opt/bizlegal/bizlegal-ea`. 🔶CONFIRMED — rotate, move to credential helper.
- No backup system of any kind; no logrotate on `ops_alerts.log` (1.1 MB, +403 spam), `seo-agents.log`, `pipeline.log`.

## Content flywheel — CONFIRMED DEAD, blocker identified
- Scout (06:44 today): `404 Not Found` on `https://curator.bizlegal-ai.com/api/chat`, `0/12 items passed`, exit. **The `/api/chat` 404 behind CF Zero Trust IS the hop-1 killer** (memory had it right).
- `bizlegal-ea/content/blog` newest post = **Jun 22 23:14** (46+ days stale). `/opt/bizlegal/curator/seo-agents/blog_content` newest = Jul 27.
- **No `gemma` on ollama** (`mistral-nemo:12b`, `llama3.2:3b` only). Hop-1 fix: repoint the Ollama tunnel (`OLLAMA_TUNNEL_URL→127.0.0.1:11434`) + clear the CF Zero Trust block on `/api/chat`.

## Resource / cost leaks
- **`openclaw-gateway`** (bizlegal PID, started Aug 30): ~22% CPU + **1.5 GB RSS**, 2d16h CPU in ~12d — single largest consumer. Not under systemd or pm2 (pm2 list empty; the `pm2-bizlegal` unit manages nothing). Orphaned. 🟡
- Churning no-output cron: `revenue_alerter` (1min), `self_heal` (5min), `ops_alerts` (5min, **403 every run** — matches OPS_DASHBOARD_TOKEN resync item in Moses ops), `code_fixer` (30min), `recovery_active` (30min).
- **HALTED-outreach live again**: `# [HALTED-2026-07-10]` jobs (marketing_outreach 0 10, signal_scout 0 1, cold_email_*, oci_deal_closer, reddit_outreach, headhunter…) are commented out in the manual block BUT re-added active inside the `# BEGIN bizlegal-managed` cron_installer block. 🔶CONFLICT, no owner.

## Health
- `curator-publisher`: HEALTHY (/health 200, last 11:00). ✅
- `curator-bot`: DEGRADED — Telegram `Bad Gateway` (Sep 11 01:10). 🟡
- Root crontab ≈ 30 manual + ~17 bizlegal-managed + recovery_active/2h. Curator timers: scout 06:00, auto-pick 10:00 (idle, 0 rows).
- n8n (up 4mo) + marimo (up 4mo) fine; `/opt/bizlegal/.env` root-owned, 738 B (values actually live in `curator/.env`).

## Bottom line for the map
This elevates **security/firewalling** from a finding to a top-3 immediate action: the payment path and the entire `decisions/` corpus sit on a box with public ollama/netdata + plaintext compose secrets + a live ghp_ token. Moses ops adds: rotate n8n compose secrets, rotate the box's ghp_ token, decide fate of openclaw-gateway (kill or systemd-wrap), re-firewall (ufw allow: 22/80/443/5678@locked-basic-auth), and reconcile the HALTED-vs-active cron conflict.
