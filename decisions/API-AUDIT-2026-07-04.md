# API AUDIT — what's working, what's missing, what's expired
**Date:** 2026-07-04 21:48 UTC+3
**Tool:** services/agents/env_audit.py
**Cron:** 0 9 * * * (just added on Hetzner)
**Live results:** 9 APIs probed, 4 OK, 5 BAD

================================================================
EXECUTIVE SUMMARY
================================================================
The system has 4 working critical APIs and 5 broken/expired/missing
ones. The 4 working ones (Anthropic, Firecrawl, Apify, Telegram)
are enough to run the core intelligence loop. The 5 broken ones
(Stripe, PayPal, Resend, NOWPayments, Perplexity) block 4 specific
revenue + content streams:

  - Stripe + PayPal = no live payment processing (Compliance
    Snapshot + Risk Snapshot + Custom Builds)
  - Resend = no email sending (cold outreach, lead_nurture, newsletter)
  - NOWPayments = no crypto payments
  - Perplexity = no GEO citation check (breaks AEO/AEO posts)

Without Stripe, the system cannot capture a single dollar of
revenue from any product. That is the #1 priority.

================================================================
THE 9 PROBES — VERBATIM RESULTS
================================================================
  Service         Status              HTTP   Action needed
  ----------------------------------------------------------------
  Anthropic       ok                  200    None
  Firecrawl       ok                  200    None
  Apify           ok                  200    None
  Telegram        ok                  200    None (@Bizlegalbot)
  ----------------------------------------------------------------
  Resend          expired_or_invalid  403    rotate key
  NOWPayments     expired_or_invalid  403    rotate key
  Stripe          expired_or_invalid  401    rotate key (CRITICAL)
  PayPal          expired_or_invalid  401    rotate creds
  Perplexity      MISSING             --     add to .env + vault
  ----------------------------------------------------------------

================================================================
DETAILED API STATUS
================================================================

WORKING (4)
-----------
  [OK] ANTHROPIC_API_KEY
       Endpoint: https://api.anthropic.com/v1/messages
       Test: POST claude-haiku-4-5, max_tokens=5, "ping"
       Response: 200, model=claude-haiku-4-5
       Powers: all 8 WAT agents, QualifierChat, Compliance
               Snapshot, Risk Snapshot, daily reports.
       Verdict: HEALTHY.

  [OK] FIRECRAWL_API_KEY
       Endpoint: https://api.firecrawl.dev/v1/search
       Test: {"query":"ping","limit":1}
       Response: 200
       Powers: signal_scout, headhunter enrichment, risk-snapshot
               scrape, lead research.
       Verdict: HEALTHY. Hobby tier = 500 credits/mo; monitor
               usage via /api/ops/command.

  [OK] APIFY_API_TOKEN
       Endpoint: https://api.apify.com/v2/users/me
       Response: 200, user=bizlegal-ai
       Powers: LinkedIn job scraper (headhunter), other actors
       (APIFY_ACTOR_CIPA / GPC / SURPLUS set in .env).
       Verdict: HEALTHY. Corrects the prior session's claim that
               APIFY was missing — it is set.

  [OK] TELEGRAM_BOT_TOKEN
       Endpoint: https://api.telegram.org/bot{tok}/getMe
       Response: 200, bot=@Bizlegalbot
       Powers: curator-bot systemd unit, ops_alerts, ea_agent.
       Verdict: HEALTHY.

BROKEN (5)
----------
  [BAD] STRIPE_SECRET_KEY  *** INVALID ***
       Endpoint: https://api.stripe.com/v1/balance
       Response: 401 "Invalid API Key provided: sk_live_..."
       Blocked revenue lines:
         - Compliance Snapshot $9 unlock (1c5f0c2)
         - Risk Snapshot $19 (3f4f9aa, this commit)
         - Custom Build Pilot $2,500 / Build $15,000 / Flagship $40K
         - LexAudit $99/mo subs
       Fix: rotate key at https://dashboard.stripe.com/apikeys
            - mint new SECRET key (rk_live_*** + sk_live_***)
            - append to vault AND Hetzner .env AND Vercel env
            - re-run env_audit at 09:00 UTC tomorrow
       Impact: $0 revenue possible today.

  [BAD] PAYPAL_CLIENT_ID + PAYPAL_CLIENT_SECRET  *** INVALID ***
       Endpoint: https://api-m.paypal.com/v1/oauth2/token
       Response: 401 "invalid_client"
       Blocked: PayPal checkout for DocAI ($97) and LexAudit ($99/mo)
       Fix: rotate at https://developer.paypal.com/dashboard/applications
            - new Client ID + Secret
            - update both vault + Hetzner + Vercel

  [BAD] RESEND_API_KEY  *** INVALID ***
       Endpoint: https://api.resend.com/domains
       Response: 403 "error code: 1010"
       Blocked: ALL email sending
         - cold_email_sender.py (drafts staged, no delivery)
         - lead_nurture.py (drip campaigns)
         - newsletter_agent.py
         - invoice cron (Resend reminder emails)
         - ea_agent.py (Telegram IS via bot token, OK, but
                       daily report email is broken)
       Fix: rotate at https://resend.com/api-keys
            Also: verify RESEND_AUDIENCE_ID is set (it is, per
            Hetzner .env probe). Add the rotated key to both
            vault and Hetzner .env and Vercel env.

  [BAD] NOWPAYMENTS_API_KEY  *** INVALID ***
       Endpoint: https://api.nowpayments.io/v1/currencies
       Response: 403 "error code: 1010"
       Blocked: crypto payment checkout (BTC/ETH/USDT for DocAI)
       Fix: rotate at https://account.nowpayments.io/settings/api

  [BAD] PERPLEXITY_API_KEY  *** MISSING from Hetzner .env ***
       Endpoint: https://api.perplexity.ai/chat/completions
       Response: 401
       Status: key IS in vault, just NOT in Hetzner .env
       Blocked: geo_citation cron (07:00 UTC) returns 0 citations;
                breaks the AEO/AEO post strategy
       Fix: 1-line
            echo "PERPLEXITY_API_KEY=<value>" >> /opt/bizlegal/curator/.env
            systemctl restart curator-bot
       Impact: all 7-day AEO calendar posts produce 0 citation
               lift until the key is in .env.

================================================================
THE 3 THINGS BLOCKING REVENUE (in priority order)
================================================================
  1. STRIPE_SECRET_KEY
     No Stripe = no payment of any kind.
     $0/day in revenue until rotated.
     Action: rotate NOW in Stripe dashboard. 5 min.

  2. RESEND_API_KEY
     No Resend = no email of any kind.
     All nurture + outreach + invoices are dead in the water.
     Action: rotate at resend.com. 5 min.

  3. PAYPAL_CLIENT_ID + SECRET
     Blocks the existing DocAI ($97) + LexAudit ($99/mo) sales
     that have been "live" but never actually transacted.
     Action: rotate at developer.paypal.com. 10 min.

  Optional: NOWPayments (crypto) + Perplexity (GEO)
  NOWPayments = nice to have for crypto buyers
  Perplexity = 1-line fix (.env only)

================================================================
MISSING FROM VAULT (intentionally? or forgotten?)
================================================================
  The Hetzner .env has 60+ keys that are NOT in the local copy
  of the vault. The local vault is at /root/env-hub-bizlegal-ai.txt
  but the canonical vault is at:
    C:\Users\Moshe Dor\Downloads\env-hub-bizlegal-ai.txt

  These two files are out of sync. The .env on Hetzner has the
  keys that the running system needs, but they're not in the
  Windows vault for some reason — possibly because they were
  added directly to .env without going through the O5 standing
  order ("vault before commit").

  Recommendation: sync vault <-> .env via the next session.
  env_audit.py has a `missing_from_vault` array — it will keep
  reporting these until you sync.

================================================================
WHAT THIS MEANS FOR THE PLAN
================================================================
  The 24/7 Revenue Machine plan's "Day-7 = first paying customer"
  target requires Stripe + Resend to be working. Both are broken.
  Per the cost ledger:
    - 1 Pilot @ $2,500 covers 12-16 months of system cost
    - 22 Snapshot customers @ $9 covers 1 month
  The 5 broken APIs collectively block ~$0/day in revenue right
  now. Rotating the 3 critical ones (Stripe, Resend, PayPal) takes
  20 minutes and unblocks every revenue line.

================================================================
HERMES TIME SAVINGS — what the env_audit cron saves
================================================================
  Before: weekly manual check of 9 APIs = ~15 min/week = 1 hr/mo
  After: 9 APIs checked in 6 sec every morning at 09:00 UTC
          + persisted to agent_runs for historical trending
  Savings: ~12 hr/yr of manual API checking
  + catches expired keys BEFORE they break a customer-facing
    flow (the "customers get 500 error" failure mode)

================================================================
FILES TOUCHED THIS SESSION (commit 3f4f9aa)
================================================================
  services/agents/signal_scout.py            (10.2 KB) Apollo dropped
  services/agents/env_audit.py                (6.3 KB) NEW
  services/apply_pending_migrations.py        (1.9 KB) NEW
  apps/hub/app/api/ops/command/route.ts       (5.0 KB) NEW (WP6)
  apps/hub/app/ops/command/page.tsx           (0.4 KB) NEW (WP6)
  apps/hub/app/ops/command/client.tsx         (6.8 KB) NEW (WP6)
  apps/hub/app/api/risk-snapshot/generate/
                          route.ts            (6.3 KB) NEW (WP5)
  apps/hub/app/products/risk-snapshot/
                          client.tsx          (7.6 KB) NEW (WP5)
  apps/hub/app/products/risk-snapshot/
                          page.tsx            (0.8 KB) NEW (WP5)
  apps/hub/components/conversion/
                  QualifierChat.tsx            (4.5 KB) NEW (WP3 done)
  supabase/migrations/20260704_risk_snapshots.sql
                                            (1.2 KB) NEW
  decisions/COST-LEDGER-2026-07-04.md (last commit, unchanged)
  13 files, 1 commit, pushed to origin/main

================================================================
WHAT'S LEFT TO DO (the actual action list)
================================================================
  P0 (20 min, blocks all revenue)
  ---------------------------------
  1. Rotate STRIPE_SECRET_KEY in Stripe dashboard
     Append new key to: vault + Hetzner .env + Vercel env
  2. Rotate RESEND_API_KEY at resend.com
     Append to: vault + Hetzner .env + Vercel env
  3. Rotate PAYPAL_CLIENT_ID + SECRET at developer.paypal.com
     Append to: vault + Hetzner .env + Vercel env

  P1 (5 min, easy)
  -----------------
  4. Append PERPLEXITY_API_KEY to /opt/bizlegal/curator/.env
     (key is already in vault, just copy to .env)
     Restart curator-bot

  P1 (5 min, Hetzner-only)
  ------------------------
  5. Apply deal_rooms + risk_snapshots migrations to Supabase
     (requires SUPABASE_DB_URL — Moses must add this to .env
     AND the running apply_pending_migrations.py will pick them
     up on the next run)

  P2 (DNS, 5 min, you do this in browser)
  ---------------------------------------
  6. Add hub.bizlegal-ai.com CNAME in Cloudflare
     CNAME hub -> cname.vercel-dns.com (proxied)
     Then trigger Vercel redeploy

  P3 (next session, 2 hr)
  -----------------------
  7. Add Supabase DB password to vault + .env as SUPABASE_DB_URL
  8. Run apply_pending_migrations.py
  9. Sync vault <-> Hetzner .env (one-time reconciliation)
