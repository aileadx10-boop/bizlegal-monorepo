{
  "name": "@bizlegal/deal-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Transaction reconciliation core: normalise, reconcile, jurisdiction packs. Pure and deterministic — no LLM, no network.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo ok",
    "test": "node tests/run.cjs"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.7.2"
  }
}
