{
  "name": "@bizlegal/rate-limit",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight in-memory sliding-window rate limiter. Phase AA D10 (SECURITY-V3 C-1 mitigation) — backstop layer until Turnstile is provisioned. No Upstash / Redis dependency.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo ok",
    "test": "echo 'no tests yet'"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.7.2"
  }
}
