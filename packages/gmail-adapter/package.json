{
  "name": "@bizlegal/gmail-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Google Gmail send/receive + label adapter for BrainX (gws pattern, Resend fallback).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "import": "./src/index.ts", "default": "./src/index.ts" }
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo ok",
    "test": "tsx --test src/gmail.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2"
  }
}
