# content/brainx

- `aeo-geo-seed.md` — five planned "correct-the-AI" landing-page angles
  (`/wrong-ai/...`), meant to auto-publish once an opportunity reaches
  `validate` status. **Not built** — none of those routes exist yet. Revisit
  once the weekly radar (`agents/brainx/radar-run/SOP.md`) has produced real
  `validate`-tier opportunities to publish from.

BrainX's actual public guide content lives inside the app itself, at
`apps/brainx/content/guides/` — see `apps/brainx/CLAUDE.md` for why (Vercel
file-tracer scope), not here.
