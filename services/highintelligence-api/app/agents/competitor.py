"""Competitor Intelligence agent entrypoint."""
from __future__ import annotations

def run(snapshot: dict) -> dict:
    # Placeholder: hash/diff via Neon, emit detected_changes.
    return {"agent": "competitor", "changes": []}
