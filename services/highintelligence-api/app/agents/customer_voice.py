"""Customer Voice agent entrypoint."""
from __future__ import annotations

def run(signals: list[dict]) -> dict:
    # Placeholder: classifier + voice extraction + clustering.
    return {"agent": "customer_voice", "voices": 0}
