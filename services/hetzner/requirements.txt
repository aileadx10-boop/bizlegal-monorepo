# Hetzner curator — Python deps.
# Most are already in /opt/bizlegal/venv from setup.sh.
# `pip install -r requirements.txt` is idempotent — safe to re-run.

requests>=2.32.0
supabase>=2.7.0
python-dotenv>=1.0.1
anthropic>=0.39.0
feedparser>=6.0.11
beautifulsoup4>=4.12.3
lxml>=5.3.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Telegram bot — long-poll based listener (no webhook required).
python-telegram-bot>=21.6

# Image gen via OpenAI (no SDK; we use raw httpx). httpx already a sub-dep.
httpx>=0.27.0

# Mermaid diagram validator. Not strictly required if mermaid-cli installed
# at the OS level, but we ship a Python fallback that just shells out to npx.
# (Drop this line if mermaid-cli is already in PATH from the setup.sh step.)
